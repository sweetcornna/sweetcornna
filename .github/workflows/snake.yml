name: Generate Snake Animation

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: Platane/snk/svg-only@v3
        with:
          github_user_name: sweetcornna
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#EC4899&color_dots=#1A1B27,#4C1D95,#7C3AED,#A855F7,#EC4899
            dist/github-contribution-grid-snake-dark.svg?color_snake=#EC4899&color_dots=#1A1B27,#4C1D95,#7C3AED,#A855F7,#EC4899

      - uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
